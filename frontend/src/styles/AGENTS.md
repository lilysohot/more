# Styles 模块规则

适用目录：`frontend/src/styles/`

必读：`../../docs/page-ui-patterns.md`

## 关键规则

- 全局样式、共享布局样式和报告工作台视觉系统集中放这里。
- 小范围页面布局改动优先用 Tailwind，不要为零散样式不断扩大全局 CSS。
- 报告工作台相关类名继续复用现有 `report-*` 命名体系。
- 改样式后同时检查桌面和移动端，不只看当前浏览器宽度。
